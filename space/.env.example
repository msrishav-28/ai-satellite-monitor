# Frontend runtime configuration for the canonical Next.js app in /space.
# Added during Phase 4 so local setup and scripts target the tracked frontend.

NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000/api/v1/ws
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
